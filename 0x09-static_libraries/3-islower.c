#include "main.h"

/**
 * _islower - checks for lowercase letters.
 * @c: the character to be checked.
 *
 * return: 1 for lowercase letters or 0 for otherwise.
 */

int _islower(int c)
{
return (c >= 'a' && c <= 'z')
}
